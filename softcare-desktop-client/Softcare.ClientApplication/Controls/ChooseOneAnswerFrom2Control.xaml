<UserControl 
    x:Class="EHealth.ClientApplication.Controls.ChooseOneAnswerFrom2Control"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    mc:Ignorable="d" 
    d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Button Grid.Column="0" Margin="20 20 20 0" Style="{StaticResource LargeYesButton}" Content="{Binding Answer1Title}" Click="Answer1Button_Click" />
        <Button Grid.Column="1" Margin="20 20 20 0" Style="{StaticResource LargeNoButton}" Content="{Binding Answer2Title}" Click="Answer2Button_Click" />
    </Grid>
</UserControl>
